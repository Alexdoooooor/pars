[Unit]
Description=Price Intelligence Main API
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/pars
EnvironmentFile=/var/www/pars/.env
ExecStart=/usr/bin/python3 -m uvicorn server.main:app --host 127.0.0.1 --port 8765
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
